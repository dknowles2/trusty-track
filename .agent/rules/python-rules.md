---
trigger: always_on
---

* All python code MUST have type annotations.
* All python code MUST have an associated "pytest" based test.
* All python code MUST be formatted with `ruff`.
* All python code MUST pass `mypy` checks.
* All python code MUST have appropriate docstrings.
